import { HtmlPreviewPipe } from './html-preview.pipe';

describe('HtmlPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
